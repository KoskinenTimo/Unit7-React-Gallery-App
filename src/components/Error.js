import React from 'react';

// Simple Error component to be displayed when no routes match
const Error = () => (
  <div className="photo-container">
      <h2>Error 404, no page found!</h2>
  </div>
);

export default Error;